# 火灾智能检测网站设计思路与技术要求

## 设计思路

网站定位为本地演示型“烟雾与火焰智能检测平台”。页面不做传统营销首页，而是直接呈现消防监测指挥台：顶部展示平台品牌、核心导航和模型在线状态；首屏突出实时监控画面与当前火情研判；上传检测、历史记录和运行环境作为次级工作区。

整体视觉采用克制的深色消防科技风。火焰橙用于火灾报警和关键操作，青绿色用于在线、安全和数据流状态，琥珀色用于预警。界面重点放在实时画面、告警等级、检测统计和应对措施上，避免装饰性元素干扰判断。

## 页面布局

- 顶部导航：平台名称、实时监控、上传检测、事件追踪、模型状态。
- 系统概览：模型权重、推理设备、飞书报警配置、实时任务数量。
- 首屏指挥区：左侧为 RTSP 实时检测大画面，右侧为火情研判和飞书同步操作。
- 工作区：上传图片/视频检测、历史事件列表、本地运行环境说明。
- 响应式规则：桌面端使用双栏/三栏布局，平板和手机端改为单栏，始终保持监控画面优先。

## 交互要求

- RTSP 接入表单包含摄像头名称、RTSP 地址、置信度、推理尺寸和告警冷却时间。
- 上传检测支持图片和视频模式切换，显示文件类型提示、文件名、检测中状态和结果预览。
- 火情研判展示告警等级、火焰数量、烟雾数量、最高置信度、飞书发送状态和应对措施。
- 历史记录显示告警等级标签、事件判断、来源名称和创建时间，并支持按等级筛选。
- 后端离线或模型未加载时，前端仍可正常渲染并给出清晰状态提示。

## 技术要求

- 前端使用 React + Vite，静态资源放在 `frontend/public/`。
- 后端使用 FastAPI，启动时加载一次项目根目录的 `Smoke Fire.pt`。
- 图片检测接口接收上传文件，返回标注图 URL、检测框、类别、置信度和告警等级。
- 视频检测接口接收上传视频，逐帧推理并输出标注后 MP4。
- 检测类别保持当前数据集定义：`0: smoke`，`1: fire`。
- 默认推理参数为 `conf=0.25`、`imgsz=640`，前端允许用户调整。
- 本地输出文件保存在 `outputs/web/`，不影响原有命令行脚本输出。

## 运行方式

后端：

```bash
uvicorn backend.main:app --reload --host 127.0.0.1 --port 8000
```

前端：

```bash
cd frontend
npm install
npm run dev
```

浏览器访问：

```text
http://127.0.0.1:5173
```
