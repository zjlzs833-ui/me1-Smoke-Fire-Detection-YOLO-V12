# Backend

FastAPI service for the smoke and fire detection website.

## Run

```bash
uvicorn backend.main:app --reload --host 127.0.0.1 --port 8000
```

The service expects `Smoke Fire.pt` in the project root.
