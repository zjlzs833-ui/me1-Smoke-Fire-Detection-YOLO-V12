# Frontend

React + Vite web interface for the smoke and fire detection platform.

## Run

```bash
cd frontend
npm install
npm run dev
```

The frontend calls the FastAPI backend at `http://127.0.0.1:8000` by default.
